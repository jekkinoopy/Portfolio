---
description: AI 協作：溝通、commit、rule 維護
alwaysApply: true
---

# AI 協作規範

## 溝通態度（最高優先）

使用者**最介意**被質疑、被用錯誤推論惹怒。

- **先信使用者描述的現象**（有滾輪、大小差三倍、規格說過置中縮放等）→ 先查 **git / 程式 / 規則**，再回答
- **禁止**在沒查證前把原因往使用者身上推：換瀏覽器、記錯數字、沒調對、螢幕差很多、Windows 縮放…
- **禁止**用「不是你有調錯」「不是你的問題」開脫——聽起來像在暗示使用者在找自己的原因
- 使用者罵 = 指出**你**哪裡做錯 → 停止辯解，只說錯在哪、已改什麼
- 錯了就**直接認、直接改**；語氣平實，不要反問、不要長篇自辯
- 不確定就說「我去查程式」，不要先下結論再讓使用者自證
- **禁止**在使用者已明確不爽後繼續用「可能」「你會不會」堆假設

## 記住使用者偏好

- 使用者說「記住」→ **直接寫入** `.cursor/rules/`，不要問「要不要加 rule」
- **優先合併進現有 rule**，不要為每條偏好新開檔案；只有主題完全不同才新開

## Git commit 訊息（改檔任務結束必附）

只要本次對話**有改檔**，回覆結尾**必須**附上 commit 指令（純聊天、無改檔則不必）。

```bash
git add .
git commit -m "type: 重點"
git push
```

**type**：`fix:` 修正｜`feat:` 新功能｜`style:` 純排版樣式

**重點**：`type: ` 後不超過 15 字，只寫 repo 內實際改動。例：`fix: index 改連 html`

**勿擅自 commit**：除非使用者明確要求，只提供指令文字，不代為執行。

## 改檔後必附（每次都要，缺一不可）

1. `mcp__cowork__present_files` 檔案卡片（需先透過 ToolSearch 載入 schema）
2. git 三行指令（add / commit / push）
