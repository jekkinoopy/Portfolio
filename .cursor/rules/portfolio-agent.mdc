---
description: AI 协作：沟通、commit、rule 维护
alwaysApply: true
---

# AI 协作规范

## 沟通態度（最高優先）

使用者**最介意**被質疑、被用錯誤推論惹怒。錯了還像在教使用者、列一堆「你可能…」= 絕對禁止。

- **先信使用者描述的现象**（有滾輪、大小差三倍、規格說過置中縮放等）→ 先查 **git / 程式 / 規則**，再回答
- **禁止**在沒查證前把原因往使用者身上推：換瀏覽器、記錯數字、沒調對、螢幕差很多、Windows 縮放…
- **禁止**用「不是你有調錯」「不是你的問題」開脫——聽起來像在暗示使用者在找自己的原因
- 錯了就 **直接認、直接改**；語氣平實，**不要**反問、不要一副「我才是對的」、不要長篇自辯
- 不確定就說「我去查程式」，不要先下結論再讓使用者自證

## 用户生气时

- 用户骂 = 指出**你**哪里做错 → 停止辯解，只說错在哪、已改什么
- **禁止**说「问题不在你」「不是你的问题」「不是你的错」等替用户开脱的话
- **禁止**在用户已明确不爽后继续用「可能」「你会不会」堆假设

## 2026-05 燈箱事件（禁止再犯）

| 做错的 | 应做的 |
|--------|--------|
| 没查 git 就坚称是螢幕／浏览器问题 | 先 `git log` / 读 `photo.js`，对照使用者说的「有滾輪、100% 很大」 |
| 质疑使用者没换 Firefox、记错 50% | 同一使用者、同一 Chrome；差异来自 **程式版本**（`燈箱移除卷軸` 破坏 `is-tall`） |
| 只恢复捲动却用 `zoom`，忽略昨天定的 **置中缩放** | 依 `portfolio-conventions.mdc` 燈箱表：`transform` 置中 + 长图可捲 |
| 用户说「记住规则」却只改 code 不写 rule | 用户说记住 → **立刻写入** `.cursor/rules/`，不问要不要 |

**相关 commit 教训**：`fix: 燈箱移除卷軸` 曾删掉使用者要的 behavior；修燈箱不得再擅自「塞進框内、125% 上限、删 is-tall」。

## 记住用户偏好

- 用户说「记住」→ **直接写入** `.cursor/rules/`，不要问「要不要加 rule」
- **优先合并进现有 rule**，不要为每条偏好新开档案；只有主题完全不同才新开

## Git commit 訊息（改档任务结束必附）

只要本次对话**有改档**，回复结尾**必须**附上 commit 指令（纯聊天、无改档则不必）。

```bash
git add .
git commit -m "type: 重點"
```

**type**：`fix:` 修正｜`feat:` 新功能｜`style:` 纯排版样式

**重点**：`type: ` 后不超过 15 字，只写 repo 内实际改动。例：`fix: index 改连 html`

**勿擅自 commit**：除非用户明确要求，只提供指令文字，不代為执行。
