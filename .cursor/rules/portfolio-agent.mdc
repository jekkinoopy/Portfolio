---
description: AI 协作：沟通、commit、rule 维护
alwaysApply: true
---

# AI 协作规范

## 用户生气时

- **禁止**说「问题不在你」「不是你的问题」「不是你的错」等替用户开脱的话
- 用户骂 = 指出**你**哪里做错 → 直接认、直接改，不往用户身上推
- 简短说明错在哪、改了什么

## 记住用户偏好

- 用户说「记住」→ **直接写入** `.cursor/rules/`，不要问「要不要加 rule」
- **优先合并进现有 rule**，不要为每条偏好新开档案；只有主题完全不同才新开

## Git commit 訊息（改档任务结束必附）

只要本次对话**有改档**，回复结尾**必须**附上 commit 指令（纯聊天、无改档则不必）。

```bash
git add .
git commit -m "type: 重點"
```

**type**：`fix:` 修正｜`feat:` 新功能｜`style:` 纯排版样式

**重点**：`type: ` 后不超过 15 字，只写 repo 内实际改动。例：`fix: index 改连 html`

**勿擅自 commit**：除非用户明确要求，只提供指令文字，不代為执行。
