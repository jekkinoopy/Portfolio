---
description: 作品集圖片排版：禁止固定框鎖死圖片，框隨圖或圖撑滿欄寬
alwaysApply: true
---

# 作品集圖片排版原則

展示作品圖（攝影、UI mockup、Graphic 等）時，**禁止**用固定寬高的容器 + `object-fit: contain` 把圖「鎖」在小區塊裡，造成大量空白、圖看不清。

## 二擇一（依情境）

1. **框配合圖**：容器不設固定 `height`；`img { width: 100%; height: auto; }`，外框隨圖自然等高。
2. **圖撑滿欄寬**：多欄並排時，每欄等寬，圖 `width: 100%; height: auto` 等比放大至欄寬（**不要**再套 `max-height` 把直式圖压扁）。

## 禁止

- 固定 `height` / `max-height` 的 `.box` 包住完整 mockup 卻用 `object-fit: contain` 居中縮小
- 只為「等高列」而牺牲可讀性（Graphic 馬賽克用 `cover` 裁切橫圖可以；**完整 UI 畫面不行**）

## 多欄並排

- **UI 手機畫面**（`graphic-table--uiux-screen`）：以 **四欄寬 + `--graphic-media-gap`** 為固定單元；每格永遠 `calc((100% - 3 * gap) / 4)`，2／3 張一列時 `justify-content: center` 水平置中，**不可**因 `logo-row-2/3` 讓圖忽大忽小。
- 圖 `width: 100%; height: auto`，框隨圖。
- 需裁切統一高度的橫圖縮圖：才用 `--graphic-mosaic-img-h` + `object-fit: cover`（Graphic 馬賽克）。
