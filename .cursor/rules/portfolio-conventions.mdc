---
description: 作品集排版与文案约定
alwaysApply: true
---

# 作品集约定

## 图片排版

展示作品图（摄影、UI mockup、Graphic 等）时，**禁止**用固定宽高的容器 + `object-fit: contain` 把图锁在小区块里，造成大量空白。

**二择一**：

1. **框配合图**：容器不设固定 `height`；`img { width: 100%; height: auto; }`
2. **图撑满栏宽**：多栏并排时，每栏等宽，图 `width: 100%; height: auto`（**不要**再套 `max-height` 把直式图压扁）

**禁止**：固定 `height` / `max-height` 的 `.box` 包住完整 mockup 却用 `object-fit: contain` 居中缩小

**UI 手机画面**（`graphic-table--uiux-screen`）：以四栏宽 + `--graphic-media-gap` 为固定单元；2／3 张一列时 `justify-content: center`，**不可**因 `logo-row-2/3` 让图忽大忽小。需裁切统一高度的横图缩图才用 `object-fit: cover`（Graphic 马赛克）。

## 文案对齐

除非用户**明确指定**，依同一段落的可见字数判断（中文一字算一字）：

| 同段字数 | 对齐 |
|----------|------|
| ≤10 字 | `text-align: center` |
| >10 字 | `text-align: left` |

窄宽区块（如 `max-width: 72ch`）长段落：区块 `margin-inline: auto`，文字靠左。短标（≤10 字）区块与文字皆可置中。

**禁止**：长段落整段居中（除非用户要求）；短标题无故靠左与下方置中图片不对齐。

## 燈箱（`photo-lightbox` / `z_assets/js/photo.js`）

使用者已定規格，**不得**擅自改回「整圖塞進框內、無捲動、上限 125%」。

| 項目 | 規則 |
|------|------|
| 縮放方式 | **置中縮放**：`#photo-lightbox-img` 使用 `transform-origin: center` + `translate` + `scale` |
| 禁止 | `frame.style.zoom` 或刪除 `is-tall` CSS 後硬壓長圖 |
| 一般橫圖 | stage 內 flex **置中**；100% 依 `max-height: calc(92vh - 88px)` |
| 長圖 | 高寬比 ≥ 1.4 → `is-tall`：以**寬度**為主、高度可超出、stage `overflow: auto` **可捲** |
| 放大後 | scale > 1 → `is-zoomed`、可**拖曳平移** |
| 縮放範圍 | 50%～400%，步進 0.25；Ctrl／⌘＋滾輪縮放；長圖未放大時滾輪用來捲動 |
| 操作提示 | 工具列保留：長圖可捲動 · Ctrl＋滾輪縮放 · 拖曳平移 · 雙擊重設 |

曾有一次錯誤提交 `fix: 燈箱移除卷軸` 破壞上述行為；修燈箱時以 **`feat: 燈箱可縮放捲動`** 的置中 transform 為準，不要只恢復捲動卻改用 `zoom`。
