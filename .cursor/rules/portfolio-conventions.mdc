---
description: 作品集排版與文案約定
alwaysApply: true
---

# 作品集約定

## 圖片排版

展示作品圖（攝影、UI mockup、Graphic 等）時，**禁止**用固定寬高的容器 + `object-fit: contain` 把圖鎖在小區塊裡，造成大量空白。

**二擇一**：

1. **框配合圖**：容器不設固定 `height`；`img { width: 100%; height: auto; }`
2. **圖撐滿欄寬**：多欄並排時，每欄等寬，圖 `width: 100%; height: auto`（**不要**再套 `max-height` 把直式圖壓扁）

**禁止**：固定 `height` / `max-height` 的 `.box` 包住完整 mockup 卻用 `object-fit: contain` 居中縮小

**UI 手機畫面**（`graphic-table--uiux-screen`）：以四欄寬 + `--graphic-media-gap` 為固定單元；2／3 張一列時 `justify-content: center`，**不可**因 `logo-row-2/3` 讓圖忽大忽小。需裁切統一高度的橫圖縮圖才用 `object-fit: cover`（Graphic 馬賽克）。

## 文案對齊

除非使用者**明確指定**，依同一段落的可見字數判斷（中文一字算一字）：

| 同段字數 | 對齊 |
|----------|------|
| ≤10 字 | `text-align: center` |
| >10 字 | `text-align: left` |

窄寬區塊（如 `max-width: 72ch`）長段落：區塊 `margin-inline: auto`，文字靠左。短標（≤10 字）區塊與文字皆可置中。

**禁止**：長段落整段居中（除非使用者要求）；短標題無故靠左與下方置中圖片不對齊。

## 燈箱（`photo-lightbox` / `z_assets/js/photo.js`）

使用者已定規格，**不得**擅自改回「整圖塞進框內、無捲動、上限 125%」。

| 項目 | 規則 |
|------|------|
| 縮放方式 | **置中縮放**：`#photo-lightbox-img` 使用 `transform-origin: center` + `translate` + `scale` |
| 禁止 | `frame.style.zoom` 或刪除 `is-tall` CSS 後硬壓長圖 |
| 一般橫圖 | stage 內 flex **置中**；100% 依 `max-height: calc(92vh - 88px)` |
| 長圖 | 高寬比 ≥ 1.4 → `is-tall`：以**寬度**為主、高度可超出、stage `overflow: auto` **可捲** |
| 放大後 | scale > 1 → `is-zoomed`、可**拖曳平移** |
| 縮放範圍 | 50%～400%，步進 0.25；Ctrl／⌘＋滾輪縮放；長圖未放大時滾輪用來捲動 |
| 操作提示 | 工具列保留：長圖可捲動 · Ctrl＋滾輪縮放 · 拖曳平移 · 雙擊重設 |

曾有一次錯誤提交 `fix: 燈箱移除卷軸` 破壞上述行為；修燈箱時以 **`feat: 燈箱可縮放捲動`** 的置中 transform 為準，不要只恢復捲動卻改用 `zoom`。
